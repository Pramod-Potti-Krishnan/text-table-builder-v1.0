# Text and Table Content Builder v1.0 - Environment Variables
# Copy this file to .env and configure for your environment

# Server Configuration
PORT=8001
HOST=0.0.0.0
LOG_LEVEL=info
RELOAD=false

# CORS Configuration
ALLOWED_ORIGINS=*

# LLM Provider Configuration
# Options: gemini, openai, anthropic
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.5-flash
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=60000

# v3.3 Security Update: Gemini via Vertex AI with ADC
# =====================================================
# This project uses Application Default Credentials (ADC) for secure authentication.
# The SAME CODE works in two environments without changes:
#
# LOCAL DEVELOPMENT:
#   1. Run: gcloud auth application-default login
#   2. Set GCP_PROJECT_ID below
#   3. DO NOT set GCP_SERVICE_ACCOUNT_JSON
#   4. App will automatically use your personal gcloud credentials
#
# RAILWAY PRODUCTION:
#   1. Set all three variables in Railway dashboard
#   2. GCP_SERVICE_ACCOUNT_JSON should contain the full service account JSON
#   3. App will automatically detect and use the service account
#
# See SECURITY.md for detailed setup instructions.

# GCP Project Configuration (REQUIRED for both local and Railway)
GCP_PROJECT_ID=deckster-xyz
GCP_LOCATION=us-central1

# GCP Service Account JSON (ONLY for Railway/Production)
# DO NOT set this locally - ADC will be used instead
# For Railway: Paste the full JSON content from your service account key file
# Example: GCP_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"deckster-xyz",...}
# GCP_SERVICE_ACCOUNT_JSON=

# OpenAI Configuration (Optional)
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-4-turbo-preview

# Anthropic Configuration (Optional)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# ANTHROPIC_MODEL=claude-3-sonnet-20240229

# Session Management
SESSION_CACHE_TTL=3600  # 1 hour in seconds
SESSION_MAX_HISTORY=5  # Max slides to keep in context
USE_REDIS=false  # Set to true for distributed sessions

# Redis Configuration (Optional)
# REDIS_URL=redis://localhost:6379/0
# REDIS_PASSWORD=

# Word Count Configuration
WORD_COUNT_TOLERANCE=0.10  # ±10% variance allowed
WORD_COUNT_ESTIMATE_PER_CHAR=0.15  # Chars to words conversion

# HTML Configuration
HTML_ALLOWED_TAGS=p,strong,em,mark,code,ul,ol,li,h1,h2,h3,h4,h5,h6,blockquote,pre,span,table,thead,tbody,tr,th,td
HTML_ENABLE_CUSTOM_CLASSES=true

# Performance
MAX_CONCURRENT_REQUESTS=10
REQUEST_TIMEOUT_SECONDS=30

# Monitoring
# SENTRY_DSN=your_sentry_dsn_here
LOG_FORMAT=json
